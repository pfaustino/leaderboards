{
  "name": "leaderboards",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "npm run lint && npm test",
    "lint": "eslint api lib tests --max-warnings 0",
    "test": "vitest run",
    "init-db": "node scripts/init-db.mjs"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "vitest": "^4.1.9"
  }
}
